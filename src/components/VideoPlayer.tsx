import type React from "react";

const VideoPlayer: React.FC = () => {
  return (
    <div className="w-full aspect-video bg-black font-bold w-350">
      {/* Placeholder for video player */}
    </div>
  );
};

export default VideoPlayer;
